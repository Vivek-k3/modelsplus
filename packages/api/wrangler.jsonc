{
  // Cloudflare Worker configuration for modelsplus API
  "name": "modelsplus-api",
  "main": "src/worker.ts",
  "compatibility_date": "2024-09-23",
  "compatibility_flags": ["nodejs_compat"],

  // Build configuration
  "build": {
    "command": "bun run generate && bun run build"
  }
}
